import 'package:flutter/material.dart';

class MissionsLoadingView extends StatelessWidget {
  const MissionsLoadingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Loading"),
      ),
    );
  }
}
//missions loading